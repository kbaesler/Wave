<Window x:Class="Wave.Extensions.Esri.Tests.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="clr-namespace:Wave.Extensions.Esri.Tests.UI"
        Title="MainWindow" Height="350" Width="525">
    <Window.DataContext>
        <ui:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Button Grid.Row="1" Content="Tokenized Text Box" Command="{Binding TokenizedTextBoxCommand}"/>
        <Button Grid.Row="3" Content="Watermarks" Command="{Binding WatermarksCommand}"/>
    </Grid>
</Window>
